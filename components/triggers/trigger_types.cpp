#include "trigger_types.h"

// All current ITrigger implementations are header-only or in their own
// .cpp; this translation unit reserves a slot for future shared helpers
// (e.g. JSON (de)serialisation of the base fields).
